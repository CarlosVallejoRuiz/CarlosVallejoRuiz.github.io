---
// No props — content is hardcoded per CLAUDE.md spec
---

<section class="hero fade-in">
  <h1 class="hero-name" aria-label="juank">
    <span class="name-juank typing-cursor" aria-hidden="true">juank</span>
    <span class="name-ca" aria-hidden="true">ca</span>
  </h1>

  <hr class="hero-divider" />

  <p class="hero-label">
    Juan Carlos Vallejo Ruiz / Full-Stack Dev &amp; AI Builder
  </p>

  <div class="hero-bio">
    <p>
      I build things with AI — tools that solve real problems,
      automate the tedious, and make developers' lives a little less painful.
    </p>
    <p>
      Passionate about turning ideas into products, fast.
      If you have something worth building, let's talk:
      <a href="mailto:juancarlosvallejo3@gmail.com" class="hero-email">juancarlosvallejo3@gmail.com</a>
    </p>
  </div>

  <div class="hero-meta">
    <p><span aria-hidden="true">📍</span> Terrassa, Barcelona</p>
    <p>→ Master's in Big Data &amp; AI in Sport · UNISPORT</p>
    <a
      href="mailto:juancarlosvallejo3@gmail.com"
      class="hero-meta-link"
    >juancarlosvallejo3@gmail.com</a>
    <a
      href="https://www.linkedin.com/in/carlos-vallejo-ruiz/"
      class="hero-meta-link social-link"
      target="_blank"
      rel="noopener noreferrer"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
      </svg>
      LinkedIn
    </a>
  </div>
</section>

<style>
  /* ============================================================
     HERO WRAPPER
     ============================================================ */
  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-12) var(--space-8) var(--space-8);
  }

  /* ============================================================
     NAME — h1
     ============================================================ */
  .hero-name {
    font-family: var(--font-display);
    line-height: 0.88;
    margin-bottom: 0;
  }

  .name-juank {
    display: block;
    font-size: var(--text-hero);
    font-weight: 900;
    font-style: normal;
    color: var(--color-ink);
  }

  .name-ca {
    display: block;
    font-size: var(--text-hero);
    font-weight: 900;
    font-style: italic;
    color: transparent;
    -webkit-text-stroke: 1px var(--color-border);
  }

  /* ============================================================
     DIVIDER
     ============================================================ */
  .hero-divider {
    border: none;
    border-top: 1px solid var(--color-border);
    width: 60%;
    margin: var(--space-6) 0;
  }

  /* ============================================================
     LABEL
     ============================================================ */
  .hero-label {
    font-family: var(--font-ui);
    font-weight: 500;
    font-size: var(--text-sm);
    color: var(--color-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: var(--space-6);
  }

  /* ============================================================
     BIO
     ============================================================ */
  .hero-bio {
    font-family: var(--font-mono);
    font-size: var(--text-base);
    line-height: 1.7;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  .hero-email {
    color: var(--color-accent);
    text-decoration: none;
  }

  .hero-email:hover {
    text-decoration: underline;
  }

  /* ============================================================
     SECONDARY DATA
     ============================================================ */
  .hero-meta {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding-bottom: 2rem;
  }

  .hero-meta p {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-muted);
  }

  .hero-meta-link {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-accent);
    text-decoration: none;
  }

  .hero-meta-link:hover {
    text-decoration: underline;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
</style>
